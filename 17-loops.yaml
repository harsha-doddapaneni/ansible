- name: loops demo
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.packages:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - posfix
    - httpd